import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // Change this to your desired primary color
    },
    secondary: {
      main: "#ecf7ed", // Change this to your desired secondary color
    },
  },
});

export default theme;
